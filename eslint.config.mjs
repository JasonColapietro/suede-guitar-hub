import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

export default defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // The interactive tools intentionally restore browser-local state after
      // hydration and move focus after conditional result panels mount. Those
      // effects synchronize with localStorage and the DOM; moving their state
      // reads into server rendering would create hydration mismatches.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  globalIgnores([".next/**", "out/**", "build/**", "next-env.d.ts"]),
]);
